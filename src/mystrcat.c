#include<unistd.h>
#include<stdlib.h>
int mystrlen (const char *str);
char* mystrcat (char *dst,const char *src)
{
        char *dst;
	char *src


        int i;
        i = 0;
        while ( dst[i] != 0))
        {
                dst[i]=src[i];
                i++;
        }


        return(dst);

}

